{
  "devDependencies": {
    "cypress": "^14.3.0"
  },
  "scripts": {
    "cy:open": "cypress open",
    "start": "npx cypress run",
    "build": "echo 'Skipping build for live website testing' && exit 0"
  }
}
